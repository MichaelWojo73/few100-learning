

describe('writing specs in typescript', () => {
    it('is easy', () => {
        expect(true).toBe(true);
    });
});

describe('doing stuff', () => {

    it('is super cool', () => {
        expect(2 + 2).toBe(4);
    });
});